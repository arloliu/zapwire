/tmp/

# Claude Code local state — agent worktrees, lock files, and per-user
# settings overrides should never be committed.
.claude/worktrees/
.claude/scheduled_tasks.lock
.claude/settings.local.json

# Go workspace files are a local dev convenience (Go team guidance: do not
# commit). Recreate with: go work init . ./otlp ./otlp/internal/conformance
go.work
go.work.sum

.vscode/settings.json
.vscode
*.code-workspace
.coverage
.benchmarks/

/*.out
/*.log
/*.test
/*.cov
/*.html
/*.pprof

